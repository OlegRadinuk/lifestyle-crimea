// Correct spelling of the sync endpoint.
// The original file lives at /api/trevelline/sync (typo).
// Both URLs are kept working for backwards compatibility.
export { GET, POST } from '@/app/api/trevelline/sync/route';
